// Sprint 5.5: AppRadius now lives in the centralized design system.
// Kept only for backward-compatible import paths. Values are numerically
// identical to the previous local scale (xs=8, sm=12, md=16, lg=24).
export '../../core/design_system/design_tokens/app_radius.dart';
